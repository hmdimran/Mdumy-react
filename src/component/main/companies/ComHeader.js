import React from "react";

export default function ComHeader() {
  const comheader = {
    title: "Trusted by over 800+ companies",
  };
  return (
    <header class="text-center my-4">
      <h1>{comheader.title}</h1>
    </header>
  );
}
